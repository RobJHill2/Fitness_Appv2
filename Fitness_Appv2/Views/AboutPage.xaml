<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Fitness_Appv2.Views.AboutPage"
             xmlns:vm="clr-namespace:Fitness_Appv2.ViewModels"
             Title="{Binding Title}">
    
    <ContentPage.BindingContext>
        <vm:AboutViewModel />
    </ContentPage.BindingContext>
    
    <ContentPage.Resources>
        <ResourceDictionary>
            <Color x:Key="Accent">#96d1ff</Color>
        </ResourceDictionary>
    </ContentPage.Resources>

    <Grid RowDefinitions="Auto,Auto,Auto,Auto, Auto, Auto, *">
        <Label Text="Welcome" TextColor="Black" FontSize="30" Grid.Row="0"/>
        
        <Label x:Name="BodyweightDisplay" Grid.Row="1"/>
        <Entry x:Name="BodyweightInput" Keyboard="Numeric" Placeholder="Input Bodyweight" Grid.Row="2"/>
        <Button x:Name="BodyweightUpdate" Pressed="BodyweightUpdate_Pressed" Text="Update" Grid.Row="3"/>

        <Label Text="Consistentcy Last Week: " Grid.Row="4"/>
        <Label x:Name="ConsistencyDisplay" Grid.Row="5"/>

        <Label x:Name="MotivationalQuote" Grid.Row="6"/>

        <Label Text="Pinned Exercises:" TextColor="Black" FontSize="20" Grid.Row="7"/>
        <CollectionView x:Name="viewPinned" Grid.Row="8">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <StackLayout>
                        <Label Text="ID"/>
                        <Label Text="{Binding Id}"
                                TextColor="Black"
                                FontSize="Medium"/>
                        <Label Text="Name"/>
                        <Label Text="{Binding XciseNameAttribute}"
                                TextColor="Black"
                                FontSize="Medium"/>
                        <Label Text="Goal"/>
                        <Label Text="{Binding GoalAttribute}"
                                TextColor="Black"
                                FontSize="Medium"/>
                        <Label Text="PB"/>
                        <Label Text="{Binding PBAttribute}"
                                TextColor="Black"
                                FontSize="Medium"/>
                    </StackLayout>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </Grid>
</ContentPage>
