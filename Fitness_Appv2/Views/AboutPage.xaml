<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Fitness_Appv2.Views.AboutPage"
             xmlns:vm="clr-namespace:Fitness_Appv2.ViewModels"
             Title="{Binding Title}">
    
    <ContentPage.BindingContext>
        <vm:AboutViewModel />
    </ContentPage.BindingContext>
    
    <ContentPage.Resources>
        <ResourceDictionary>
            <Color x:Key="Accent">#96d1ff</Color>
        </ResourceDictionary>
    </ContentPage.Resources>

    <Grid RowDefinitions="Auto,Auto,Auto,Auto">
        <ScrollView Grid.Row="0">
            <StackLayout Orientation="Vertical" Padding="30,24,30,24" Spacing="10">
                <Label Text="Welcome" FontSize="Title"/>
            </StackLayout>
        </ScrollView>
        <Label x:Name="BodyweightDisplay" Grid.Row="1"/>
        <Entry x:Name="BodyweightInput" Keyboard="Numeric" Placeholder="Input Bodyweight" Grid.Row="2"/>
        <Button x:Name="BodyweightUpdate" Pressed="BodyweightUpdate_Pressed" Text="Update" Grid.Row="3"/>
    </Grid>
</ContentPage>
